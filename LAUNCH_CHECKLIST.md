# Чеклист запуска

## Перед публичным запуском

- Проверить реальные контакты храма в footer: адрес `г. Горловка, ж/м «Строитель», ул. Ленина, 190`, телефон `+7 949 469 5683`, Telegram, Rutube и MAX уже добавлены 09.07.2026.
- Публичный Telegram на сайте: `https://t.me/BlagoVhram`.
- Публичный Rutube на сайте: `https://rutube.ru/channel/76042079/`.
- Публичный MAX-чат на сайте: `https://max.ru/join/yIcHJONjEc1WNkPHuV8VyW0LTcyRKoS82R6BjwyvDD4`.
- Перевыпустить раскрытый токен `@BlagoVhramBot` в BotFather. Новый токен не отправлять в чат и не коммитить.
- Дождаться делегирования домена на `ns1.sprinthost.ru` и `ns2.sprinthost.ru`.
- Создать сайт `благовещение-горловка.рф` в SprintHost и включить бесплатный SSL.
- Для стабильного запуска подключить тариф `X-1`: 190 ₽/месяц или 1 788 ₽/год, только после явного подтверждения оплаты.
- Собрать архив командой `npm run package:timeweb-hosting` и загрузить его содержимое в корень сайта SprintHost.
- Создать `api/config.php` из `api/config.example.php`, указать новый Telegram token и chat id только на хостинге.
- Проверить, что `GET /api/notify.php` возвращает `405`, а тестовый корректный `POST` возвращает `200`.
- Отправить тестовую записку после отметки оплаты и проверить доставку в закрытую Telegram-группу.
- Проверить QR СБП и поступление на расчетный счет.
- Согласовать текст согласия на обработку персональных данных.
- Юридически согласовать встроенный раздел обработки данных с реальными реквизитами оператора.
- Дождаться обновления DNS `благовещение-горловка.рф` и `www`.
- Проверить, что `https://благовещение-горловка.рф/` и `https://www.благовещение-горловка.рф/` открываются без предупреждений SSL.
- Проверить на мобильном и desktop: главную, записку, СБП, успешную отправку и отсутствие ошибок консоли.

## Что не делаем сейчас

- Не подключаем Robokassa.
- Не храним заявки в базе.
- Не принимаем банковские карты внутри сайта.
- Не показываем фиктивные реквизиты и контакты.
- Не подключаем VK на первом запуске.

## Юридически аккуратнее

- Формулировать платеж как пожертвование, а не продажу услуги.
- Не обещать автоматическое подтверждение оплаты, пока СБП проверяется вручную.
- Хранить секреты только на backend.
- Полный текст записки отправлять только в закрытую Telegram-группу ответственных на первом запуске.
- Не запускать боевой сбор персональных данных без политики и согласия.
